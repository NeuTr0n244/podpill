# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
dist-ssr/
build/

# Vite cache
.vite/

# Env files (contain API keys — NEVER commit)
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
*.local
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# The original 456MB GLB is kept locally only as a source of truth.
# We commit the WebP-compressed version (~34MB) which fits everywhere.
public/podcastelon.original.glb

# Helper scripts only used during development
inspect-*.mjs
analyze-*.mjs
dump-*.mjs
blender_*.py
