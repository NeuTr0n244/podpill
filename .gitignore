# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
dist-ssr/
build/

# Vite cache
.vite/

# Env files (contain API keys — NEVER commit)
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
*.local
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# The big GLB lives on Cloudflare R2, not in git (445MB > GitHub's 100MB limit).
# Set VITE_GLB_URL in .env.local (dev) or Vercel env vars (prod).
public/podcastelon.glb

# Helper scripts only used during development
inspect-*.mjs
analyze-*.mjs
dump-*.mjs
blender_*.py
